name: Weekly PR Automation

on:
  schedule:
    - cron: '0 14 * * SUN'  # 毎週日曜23:00 JST
  workflow_dispatch:

jobs:
  pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/auto_pr_merge.sh

